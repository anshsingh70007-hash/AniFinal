<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Only the staged update APK is shared, and only with the package installer.
         It lives in internal cache so no other app can swap it before install. -->
    <cache-path name="update_cache" path="updates/" />
</paths>
